.Search
  .Search__main
    = form_with(url: search_cards_path, local: true, method: :get, class: "search") do |f|
      = f.text_field :keyword, placeholder: "検索", class: "search"
      = f.submit "検索", class: "search"
.Data__table
  - @cards.each do |card| #データを一つずつ取り出す
    .Data__table__list
      .Data__table__list__id
        = card.id
      .Data__table__list__title
        = card.title
      .Data__table__list__language
        = card.language
      .Data__table__list__btn
        = link_to "編集", edit_card_path(card.id), method: :get
      .Data__table__list__btn
        = link_to "削除", card_path(card.id), method: :delete